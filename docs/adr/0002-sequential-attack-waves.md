# Model attack waves as separate missions

An attack-wave sequence contains separate ordered attack missions. Each wave
has its own attacking fleet; its survivors return, while the target's remaining
state carries into the next wave. The six-round combat limit and a universe's
bashing limit are separate rules, so the simulator must not hard-code six as a
universal wave count.
