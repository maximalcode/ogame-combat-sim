# Keep fleet provenance visible

Fleet input will come from user-provided combat and espionage reports that the
project parses. It will not infer fleet compositions from public scores or ship
counts, automate account access, or store game credentials. Public fixtures
retain combat-relevant inputs and source context while removing player names,
coordinates, and reusable report keys.
