# Start with the sourced defender wreck field

The first wreck-field implementation will model repairable destroyed ships for
the defender, report them separately from debris, and exclude them from recycler
profit. Attacker wreck fields for the General class and merging or timing of
successive wreck fields remain outside that task until current sources settle
their behavior.
